# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
  #  branches: [ master ]
  #pull_request:
  #  branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    container:
      image: perl

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Checkout AGAT
        uses: actions/checkout@v2
      - name: install dependencies
        run: |
          cpanm install Devel::Cover::Report::Coveralls
     #     conda config --add channels defaults
     #     conda config --add channels bioconda
     #     conda config --add channels conda-forge
     #     conda install perl>=5.8 perl-bioperl>=1.7 perl-extutils-makemaker r-base libdb perl-clone perl-graph perl-lwp-simple perl-statistics-r perl-json perl-carp perl-sort-naturally perl-file-share perl-file-sharedir-install perl-moose

      #conda env create -f conda_environment_AGAT.yml
      #conda activate agat
             
      #- name: Install libdb-dev
      #  run: sudo apt-get -y install libdb-dev
      #- name: Install R
      #  run: sudo apt-get -y install r-base
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it

      #- name: Checkout perl env
      #  uses: shogo82148/actions-setup-perl@v1
      #  with:
      #      perl-version: '5.30'
      #      install-modules-with: cpanm
      #      install-modules-args: --with-develop --with-configure --notest
      #      install-modules: Devel::Cover::Report::Coveralls bioperl Clone Graph::Directed LWP::UserAgent Statistics::R JSON Carp Sort::Naturally File::Share File::ShareDir::Install Moose

      #- name: install AGAT deps
      #  run: cpanm --installdeps .
      - name: Prepare for tests
         # sudo apt-get install libtest-perl-critic-perl libtest-output-perl libtest-pod-coverage-perl
        run: |
          perl Makefile.PL
          make
     # make test
      - name: run prove
        run: prove -lv t
